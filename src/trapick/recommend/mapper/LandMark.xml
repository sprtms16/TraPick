<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" 
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="trapick.recommend.mapper.LandMarkMapper">
   <cache />
   
   <select resultType="LandMark" parameterType="String" id= "listLandMark">
      select landmark.* from landmark, city
      where landmark.city_idx = city.city_idx
      and city.name = #{city_name}
   </select>
</mapper>